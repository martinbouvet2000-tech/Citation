<header class="header">
  <div class="header-inner">
    <nav class="nav" aria-label="Navigation principale">
      {%- for link in linklists[section.settings.menu].links -%}
        <a href="{{ link.url }}">{{ link.title }}</a>
      {%- endfor -%}
    </nav>
    <a href="{{ routes.root_url }}" class="logo">
      {%- if settings.logo != blank -%}
        <img src="{{ settings.logo | image_url: width: 300 }}" alt="{{ shop.name }}">
      {%- else -%}
        {% render 'logo-svg' %}
      {%- endif -%}
    </a>
    <div class="header-actions">
      <button class="menu-toggle" aria-label="Menu">
        {% render 'icon', name: 'menu' %}
      </button>
      {%- if section.settings.show_search -%}
        <a href="{{ routes.search_url }}" class="icon-btn" aria-label="Rechercher">
          {% render 'icon', name: 'search' %}
        </a>
      {%- endif -%}
      {%- if shop.customer_accounts_enabled -%}
        <a href="{{ routes.account_url }}" class="icon-btn" aria-label="{{ 'customer.account' | t }}">
          {% render 'icon', name: 'account' %}
        </a>
      {%- endif -%}
      <a href="/pages/favoris" class="icon-btn wishlist-toggle" aria-label="Mes favoris">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" xmlns="http://www.w3.org/2000/svg" aria-hidden="true"><path d="M20.84 4.61a5.5 5.5 0 0 0-7.78 0L12 5.67l-1.06-1.06a5.5 5.5 0 0 0-7.78 7.78l1.06 1.06L12 21.23l7.78-7.78 1.06-1.06a5.5 5.5 0 0 0 0-7.78z"/></svg>
        <span class="wishlist-count hidden">0</span>
      </a>
      <button class="icon-btn cart-toggle" aria-label="{{ 'accessibility.open_cart' | t }}">
        {% render 'icon', name: 'cart' %}
        <span class="cart-count {% if cart.item_count == 0 %}hidden{% endif %}">{{ cart.item_count }}</span>
      </button>
    </div>
  </div>
</header>

{% schema %}
{
  "name": "En-tête",
  "settings": [
    {
      "type": "link_list",
      "id": "menu",
      "label": "Menu",
      "default": "main-menu"
    },
    {
      "type": "checkbox",
      "id": "show_search",
      "label": "Afficher l'icône de recherche",
      "default": true
    }
  ]
}
{% endschema %}
